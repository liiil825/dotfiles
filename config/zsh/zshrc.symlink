export ZSH="$HOME/.oh-my-zsh"
ZSH_CUSTOM=~/.config/zsh # load.zsh 使用路径
source "$ZSH_CUSTOM/load.zsh"

plugins=(
    git
)
if [[ $(uname) == "Darwin" ]]; then
    plugins+=(brew)
fi

source $ZSH/oh-my-zsh.sh
