export ZSH_CUSTOM=~/.config/zsh # load.zsh 使用路径

source $ZSH_CUSTOM/path.zsh
source $ZSH_CUSTOM/zgenom.zsh

for the_file ($ZSH_CUSTOM/config/*.zsh); do
    source $the_file
done

for the_file ($ZSH_CUSTOM/alias/*.zsh); do
    source $the_file
done
source $ZSH_CUSTOM/aliases.zsh
